- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "px-3 alert alert-#{name.to_s == 'notice' ? 'success' : 'danger'}", :role => "alert"}
      = content_tag :div, msg, :id => "flash_#{name}"
      // %button.btn-close{"aria-label" => "Close", type: "button", "aria-hidden" => "true", "data-dismiss" => "alert"}
      // %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} ×